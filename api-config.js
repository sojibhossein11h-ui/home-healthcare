// Home Healthcare — public frontend API configuration
// Safe to keep this file public. NEVER place service-role/private keys here.
// After the production backend is created, replace the two empty values with
// the provider's public URL and public/anon client key.
window.HOME_HEALTHCARE_API = Object.freeze({
  provider: 'supabase',
  url: '',
  anonKey: ''
});
